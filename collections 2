from collections import defaultdict

# Read n and m
n, m = map(int, input().split())

# Create defaultdict with list as default type
groupA = defaultdict(list)

# Read words of group A and store positions (1-indexed)
for i in range(1, n + 1):
    word = input()
    groupA[word].append(i)

# Read words of group B and print results
for _ in range(m):
    word = input()
    if word in groupA:
        print(*groupA[word])
    else:
        print(-1)
